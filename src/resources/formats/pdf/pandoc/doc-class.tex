\documentclass[
$if(fontsize)$
  $fontsize$,
$endif$
$if(papersize)$
  $papersize$paper,
$endif$
$if(beamer)$
  ignorenonframetext,
$if(handout)$
  handout,
$endif$
$if(aspectratio)$
  aspectratio=$aspectratio$,
$endif$
$endif$
$for(classoption)$
  $classoption$$sep$,
$endfor$
]{$documentclass$}

$if(classicthesis)$
\PassOptionsToPackage[
$for(classicthesisoption)$
  $classicthesisoption$$sep$,
$endfor$
]{classicthesis}
\usepackage{classicthesis}
$endif$
